<h1>Friends index</h1>

<ul>
  {{#each model as |friend|}}
    <li>
      {{link-to friend.fullName 'friends.show' friend}}
      [{{link-to 'edit' 'friends.edit' friend}}]
      [<a href="#" {{action 'delete' friend}}>delete</a>]
    </li>
  {{/each}}
</ul>

<p>Total: {{model.length}}</p>

{{link-to 'Add a friend' 'friends.new'}}
